"""Inference utilities shared across SAS and CTS.

Import specific modules directly (e.g. ``from triton_shared.inference.detection import ...``)
rather than relying on this package to re-export everything.
"""

from __future__ import annotations
